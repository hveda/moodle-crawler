apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://textfile-collector:9100
    basicAuth: false
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "60s"